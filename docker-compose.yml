version: '3.8'

services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - .:/var/www/html
    env_file:
      - .env  # ✅ This will inject your .env values including Railway DB
    command: php artisan serve --host=0.0.0.0 --port=8000
